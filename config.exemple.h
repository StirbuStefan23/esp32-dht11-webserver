#ifndef CONFIG_H
#define CONFIG_H

// Rename the file in config.h and introduce your wifi ssid and password
const char* WIFI_SSID = "YOUR_WIFI_SSID";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

#endif // CONFIG_H